
import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export const TAWorkloadPanel: React.FC = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>TA Workload Panel</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Workload management content will be displayed here.</p>
      </CardContent>
    </Card>
  );
};
